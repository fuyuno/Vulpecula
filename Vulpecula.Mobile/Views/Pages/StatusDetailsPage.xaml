<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
     xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
     x:Class="Vulpecula.Mobile.Views.Pages.StatusDetailsPage"
     xmlns:mvvm="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
     Title="{Binding Title}" mvvm:ViewModelLocator.AutowireViewModel="True"
    Padding="10">
    <Grid ColumnSpacing="0">
        <Grid.Resources>
            <ResourceDictionary>
                <Style TargetType="Label">
                    <Setter Property="FontSize" Value="14" />
                </Style>
            </ResourceDictionary>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="56" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ContentView Padding="4,0" Grid.Column="0"
            Grid.Row="0">
            <Grid>
                <Image HeightRequest="48" HorizontalOptions="Start"
                    Source="{Binding UserIcon}" VerticalOptions="Start"
                    WidthRequest="48">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding OnTappedShowUserDetailsCommand}" />
                    </Image.GestureRecognizers>
                </Image>
            </Grid>
        </ContentView>
        <Grid Grid.Column="1" Grid.Row="0"
            RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" ColumnSpacing="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Label Text="{Binding UserName}" Grid.Column="0"
                    Font="Bold" FontSize="13"
                    LineBreakMode="NoWrap" />
                <Label Text="{Binding ScreenName}" Grid.Column="1"
                    LineBreakMode="TailTruncation" TextColor="Silver"
                    FontSize="12" VerticalOptions="End" />
            </Grid>
            <Label Grid.Row="1" Text="{Binding Text}" />
            <Label Grid.Row="2" Text="{Binding Via}" />
        </Grid>
        <Grid Grid.Row="1" Grid.Column="0"
            Grid.ColumnSpan="2" RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Text="{Binding CreatedAt}"
                TextColor="Silver" FontSize="12" />
            <Grid Grid.Row="1" HorizontalOptions="StartAndExpand">
                <StackLayout Orientation="Horizontal">
                    <StackLayout.Resources>
                        <ResourceDictionary>
                            <Style TargetType="Label">
                                <Setter Property="TextColor" Value="Silver" />
                                <Setter Property="FontSize" Value="10" />
                            </Style>
                        </ResourceDictionary>
                    </StackLayout.Resources>
                    <StackLayout Orientation="Horizontal" Spacing="2">
                        <Label Text="{Binding FavoritedCount}" />
                        <Label Text="Favs" />
                    </StackLayout>
                    <StackLayout Orientation="Horizontal" Spacing="2">
                        <Label Text="{Binding SpreadCount}" />
                        <Label Text="Shares" />
                    </StackLayout>
                </StackLayout>
            </Grid>
        </Grid>
    </Grid>
</ContentPage>